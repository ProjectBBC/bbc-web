import { BrowserRouter } from "react-router-dom";
import Root from "@containers/Root";

function App() {
    return (
        <BrowserRouter>
            <Root />
        </BrowserRouter>
    );
}

export default App;
